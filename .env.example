# Frontend - ejecución FUERA de Docker (backend local en localhost:3000)
# Vite lee este archivo como .env. El default ya apunta al backend local.
# Descomentar solo si el backend corre en otro host/puerto.
# VITE_API_BASE_URL=http://localhost:3000/api/v1
